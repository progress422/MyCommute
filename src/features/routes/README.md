# Routes feature

Connection search and route suggestion display.

## Planned responsibilities

- Search form and results UI (`components/`)
- TanStack Query hooks wrapping `searchConnections` (`hooks/`, `api/`)
- Types and Zod schemas for search params and results

## Not implemented in scaffold

Transport search, result rendering, and saving routes to favorites.
