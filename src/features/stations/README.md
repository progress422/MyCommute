# Stations feature

Station search and autocomplete support for forms.

## Planned responsibilities

- Station search input component (`components/`)
- Hook wrapping `searchStations` from shared API (`hooks/`)
- Debounced query and result types (`types/`, `schemas/`)

## Not implemented in scaffold

Station search UI, debouncing, and integration with settings/routes forms.
